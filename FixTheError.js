func square1"x" { 
    return x * x ; 
    } // func it's not knowed and the parameters must be between  () and the parameters x must be without "" ==>

    function square1(x) {
        return x*x ;
    }
    ----------------------------------
    functionsquare2 x) 
return x * x ; 
} // functionsquare2 x) are fault ==> 
     function square2(x){
        return (x*x)
     }
     ----------------------------------
     function (x) square3 { 
        return x * x;
        } // ==> the name must be before the parameters 
        function square3 (x)  { 
            return x * x;
            }
---------------------------------------
